# Application
APP_NAME=WhatsApp AI Commerce API
ENVIRONMENT=development

# PostgreSQL for a backend run directly on the host. Use the same name, user,
# and password as the root .env used by Docker Compose.
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=whatsapp_ai_commerce
DATABASE_USER=whatsapp_ai_commerce
DATABASE_PASSWORD=

# WhatsApp: configure before using webhooks/outbound messages; never commit secrets.
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
# Explicitly choose a supported Meta Graph API version (format vNN.N).
WHATSAPP_API_VERSION=
WHATSAPP_APP_SECRET=
